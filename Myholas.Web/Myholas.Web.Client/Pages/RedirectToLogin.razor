@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Как только этот компонент создается (т.е. пользователь не авторизован),
        // мы немедленно отправляем его на страницу логина
        Navigation.NavigateTo("/login", forceLoad: true);
    }
}
